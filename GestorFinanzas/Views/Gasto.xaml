<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="GestorFinanzas.Views.Gasto"
             Title="Gasto">
    <VerticalStackLayout>
        <StackLayout Padding="20">
            <Label Text="Agregar Gasto" FontSize="24" HorizontalOptions="Center" />

            <Entry Placeholder="Descripción del gasto" Text="{Binding Descripcion}" />
            <Entry Placeholder="Cantidad" Keyboard="Numeric" Text="{Binding Cantidad}" />

            <Button Text="Guardar" Command="{Binding GuardarCommand}" />
        </StackLayout>
    </VerticalStackLayout>
</ContentPage>